radius = eval(input("Enter the radius of the circle : "))
radius2 = eval(input("Enter the second radius of the circle : "))

#compute the area using the given radius from the user
area = radius * radius * 3.145

#displa the output
print("The area of the circle of radius ",radius \
, " is ",area)